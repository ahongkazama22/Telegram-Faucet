import time
import webbrowser
from telethon import TelegramClient, events
from PIL import Image

api_id = 20385404
api_hash = '7e47ea46b036b5fa01097357360bcbff'
client1 = TelegramClient('ahongkazama', api_id, api_hash)
client2 = TelegramClient('gajun', api_id, api_hash)
client3 = TelegramClient('kobot', api_id, api_hash)
client4 = TelegramClient('ibu', api_id, api_hash)
client5 = TelegramClient('andi', api_id, api_hash)
client6 = TelegramClient('endang', api_id, api_hash)
client7 = TelegramClient('gendon', api_id, api_hash)
client8 = TelegramClient('canadapandawa', api_id, api_hash)
client9 = TelegramClient('uangbulan', api_id, api_hash)
client10 = TelegramClient('lebak', api_id, api_hash)
client12 = TelegramClient('82292', api_id, api_hash)
client13 = TelegramClient('kartu', api_id, api_hash)
client14 = TelegramClient('smartfren', api_id, api_hash)
client15 = TelegramClient('pardi', api_id, api_hash)
client16 = TelegramClient('axisser', api_id, api_hash)
client17 = TelegramClient('ayemtri', api_id, api_hash)
client18 = TelegramClient('dorangireng', api_id, api_hash)
client19 = TelegramClient('dorangputeh', api_id, api_hash)
client20 = TelegramClient('kabel', api_id, api_hash)

client1.start()
for message in client1.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('ahongkazama')
          print('selesaikan captcha dulu')
           
client2.start()
for message in client2.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('gajun')
          print('selesaikan captcha dulu')
           
client3.start()
for message in client3.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('kobot')
          print('selesaikan captcha dulu')
           
client4.start()
for message in client4.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('ibu')
          print('selesaikan captcha dulu')
           
client5.start()
for message in client5.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('andi')
          print('selesaikan captcha dulu')
           
client6.start()
for message in client6.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('endang')
          print('selesaikan captcha dulu')
           
client7.start()
for message in client7.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('gendon')
          print('selesaikan captcha dulu')
           
client8.start()
for message in client8.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('canadapandawa')  
          print('selesaikan captcha dulu')
           
client9.start()
for message in client9.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('uangbulan')
          print('selesaikan captcha dulu')
           
client10.start()
for message in client10.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('lebak')
          print('selesaikan captcha dulu')
           
client12.start()
for message in client12.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print(82292)
          print('selesaikan captcha dulu')
           
client13.start()
for message in client13.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('kartu')
          print('selesaikan captcha dulu')
                      
client14.start()
for message in client14.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('smartfren')
          print('selesaikan captcha dulu')
     
client15.start()
for message in client15.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('pardi')
          print('selesaikan captcha dulu')
                
client16.start()
for message in client16.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('axisser')
          print('selesaikan captcha dulu')
                
client17.start()
for message in client17.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('ayemtri')
          print('selesaikan captcha dulu')
      
client18.start()
for message in client18.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('dorang ireng')
          print('selesaikan captcha dulu')
           
client19.start()
for message in client19.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('dorangputeh')
          print('selesaikan captcha dulu')
client20.start()
for message in client20.iter_messages('adclickersbot', limit=1):
     if message.media == None:
          pass
     else:
          print('kabel')
          print('selesaikan captcha dulu')
          
           